
spring:
  application:
    name: product-service
  profiles:
    active: prod
  cloud:
    config:
      uri: http://config-server:8888

management:
  endpoints:
    web:
      exposure:
        include: '*'
